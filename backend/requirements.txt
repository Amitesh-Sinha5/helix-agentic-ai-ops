# --- Web framework -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# --- Config / validation -----------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database ----------------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg[binary]==3.2.3
aiosqlite==0.20.0
greenlet==3.1.1

# --- Auth --------------------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1  # 4.1+ drops __about__, which passlib 1.7.4 probes for

# --- Cache / rate limiting ---------------------------------------------------
redis==5.2.1
fakeredis==2.26.2

# --- LLM providers -----------------------------------------------------------
openai==1.59.3
anthropic==0.42.0

# --- Agent orchestration -----------------------------------------------------
langgraph==0.2.60

# --- RAG ---------------------------------------------------------------------
chromadb==0.5.23
rank-bm25==0.2.2
pypdf==5.1.0

# --- Classical ML (support-triage classifier) --------------------------------
scikit-learn==1.6.0
numpy==2.2.1
joblib==1.4.2

# --- Payments ----------------------------------------------------------------
stripe==11.4.1

# --- HTTP --------------------------------------------------------------------
httpx==0.28.1

# --- Testing -----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
anyio==4.7.0
pytest-timeout==2.3.1
